#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin >> n;
	vector<int> x(n);
	vector<int> y;
	int base;
 
	for (int i = 0; i < n; i++)
		cin >> x[i];
	for (int i = 0; i < n; i++)
	{	base=500;
		while(x[i]--)
		{
			cout<<base--<<" ";
				
		}
		cout<<endl;
		
	}
		
		
}
